import { rolChecker } from './looker-rol-checker.directive';

describe('RolCheckerDirective', () => {
  it('should create an instance', () => {
    const directive = new rolChecker();
    expect(directive).toBeTruthy();
  });
});
